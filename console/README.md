# Operator Console

This directory contains the MASA operator-facing UI for escalation traces and pipeline visibility.

Development:

```bash
npm install
npm run dev
```

Production build:

```bash
npm run build
```
