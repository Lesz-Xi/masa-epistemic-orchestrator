# MASA Orchestrator v2 — Python dependencies

# Core
mcp>=1.0.0,<2.0.0
httpx>=0.27.0,<1.0.0
pydantic>=2.0.0,<3.0.0

# HTTP servers (console backend + MCP remote transport)
fastapi[standard]>=0.115.0
uvicorn>=0.32.0
starlette>=0.41.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
